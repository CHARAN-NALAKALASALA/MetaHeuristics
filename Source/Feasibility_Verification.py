import sys

def feasibility_Verification(arg):
    route = [sum1, sum2,...]

     #time iteration
    while (not feasible) or :
        time += 1
        for nodes:
            if (departure_node &time == node.start_date):
                add node to nodes_to_evacuate
            else:

        for nodes_to_evacuate:
            
        for arc in arcs:
            test(arc.capacity < number_persons)
            if(date_limite < time):
                infeasible

        All_empty_nodes = 1
        for(nodes):
            if(number_persons == 0):
                remove from nodes_to_evacuate
            else:
                All_empty_nodes
        if(All_empty_nodes == 1):
           end=1



    processing_time = time